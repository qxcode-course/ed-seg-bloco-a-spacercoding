package main
import "fmt"

func sudokinho(sequencia *string, somaDesejada int) {

    if (*sequencia)[0] == '.' {

        (*sequencia)[0] = 

    }

}

func main() {
    var sequencia string
    fmt.Scan(&sequencia)

    var somaDesejada int
    fmt.Scan(&somaDesejada)

    sudokinho(&sequencia, somaDesejada)
}